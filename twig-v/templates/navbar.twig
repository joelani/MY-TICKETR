<header class="flex justify-between items-center border-b border-gray-800 px-6 py-4">
  <h1 class="text-xl font-semibold">Dashboard</h1>
  <div class="flex items-center gap-4">
    <span>{{ user.name|default('Guest') }}</span>
    <img src="/assets/img/avatar.png" class="w-8 h-8 rounded-full" alt="User Avatar">
  </div>
</header>
